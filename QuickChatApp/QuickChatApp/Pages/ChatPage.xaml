<Page x:Class="QuickChatApp.Pages.ChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QuickChatApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ChatPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель со списком чатов -->
        <DockPanel>
            <Border DockPanel.Dock="Top" Background="#EEEEEE" Padding="10,8">
                <DockPanel>
                    <Button DockPanel.Dock="Left" Background="Transparent" BorderThickness="0" Width="30" Height="30">
                        <TextBlock Text="☰" FontSize="24" HorizontalAlignment="Center" Foreground="#8F8F8F"/>
                    </Button>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top">
                        <TextBlock Text="QuickChat" Style="{StaticResource AppTitleStyle}" Padding="0" Margin="0"/>
                        <Image Source="/Assets/Images/icon.png" Width="32" Height="35" Margin="0" />
                    </StackPanel>
                </DockPanel>
            </Border>
            <!--меню со скруглением-->
            <Border Background="#F9F9F9"
                    CornerRadius="8,0,0,8" 
                    Margin="10,0,0,8"
                    BorderBrush="#5C9EAD"
                    BorderThickness="1,1,0,1"
                    DockPanel.Dock="Top">
                <DockPanel DockPanel.Dock="Top"  Margin="10,10,4,10">
                    <DockPanel DockPanel.Dock="Top">
                        <Button DockPanel.Dock="Right" VerticalAlignment="Top" Background="Transparent" BorderThickness="0" Width="23" Height="23" Margin="5,0,0,0">
                            <!--Добавление-->
                            <Border CornerRadius="10"  Background="#5C9EAD" Width="20">
                                <TextBlock Text="+" Foreground="White" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Button>
                        <Border Background="#EEEEEE"
                                CornerRadius="4,4,4,4" 
                                Margin="0"
                                DockPanel.Dock="Top">
                            <DockPanel>
                                <Image Source="/Assets/Images/search.png" Width="15" Height="15" Margin="5" />
                                <TextBox Text="Поиск" 
                                Foreground="#8F8F8F"
                                GotFocus="SearchBox_GotFocus"
                                LostFocus="SearchBox_LostFocus"
                                 FontWeight="Normal">
                                    <TextBox.Template>
                                        <ControlTemplate TargetType="TextBox">
                                            <Border>
                                                <ScrollViewer x:Name="PART_ContentHost" 
                                                     Margin="{TemplateBinding Padding}"
                                                     VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </TextBox.Template>
                                </TextBox>
                            </DockPanel>
                        </Border>
                    </DockPanel>
                    <ListBox x:Name="ChatsList" 
                                     Style="{StaticResource ChatListStyle}"
                                     ItemContainerStyle="{StaticResource ChatItemStyle}"
                                     DockPanel.Dock="Top">
                        <ListBoxItem>
                            <!--<DataTemplate> Подправлю потом-->
                            <StackPanel Orientation="Horizontal" Margin="7">
                                <Ellipse Width="28" Height="28" Fill="#8F8F8F"/>
                                <StackPanel Margin="10,0">
                                    <TextBlock Text="Имя контакта" Foreground="#5B5B5B" />
                                    <!--TextBlock Text="{Binding Name}" Foreground="#5B5B5B"-->
                                </StackPanel>
                            </StackPanel>
                        </ListBoxItem>
                    </ListBox>
                </DockPanel>
            </Border>
        </DockPanel>

        
        
        
        
        
        
        <!-- Правая панель с чатом -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="52"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

            <Border Grid.RowSpan="3" Style="{StaticResource ChatAreaStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="60"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="80"/>
                    </Grid.RowDefinitions>

                    <!-- Заголовок чата -->
                    <DockPanel Grid.Row="0" Margin="20,15">
                        <Ellipse Style="{StaticResource AvatarStyle}" DockPanel.Dock="Left"/>
                        <StackPanel DockPanel.Dock="Left" Margin="15,0" VerticalAlignment="Center">
                            <TextBlock Text="Анна Петрова" Style="{StaticResource ChatTitleStyle}"/>
                            <TextBlock Text="в сети" Style="{StaticResource LastMessageStyle}"/>
                        </StackPanel>
                    </DockPanel>

                    <!-- Область сообщений -->
                    <ScrollViewer Grid.Row="1" 
                                  Style="{StaticResource MessagesScrollViewerStyle}"
                                  x:Name="MessagesScrollViewer">
                        <StackPanel x:Name="MessagesPanel" Margin="10">

                            <!-- Примеры сообщений -->
                            <!-- Входящее сообщение -->
                            <Border Style="{StaticResource IncomingMessageStyle}">
                                <StackPanel>
                                    <TextBlock Text="Привет! Как дела? Давно не виделись!" 
                                               Style="{StaticResource MessageTextStyle}"/>
                                    <TextBlock Text="14:32" Style="{StaticResource MessageTimeStyle}"/>
                                </StackPanel>
                            </Border>

                            <!-- Исходящее сообщение -->
                            <Border Style="{StaticResource OutgoingMessageStyle}">
                                <StackPanel>
                                    <TextBlock Text="Привет! Все отлично, спасибо! А у тебя как?" 
                                               Style="{StaticResource OutgoingMessageTextStyle}"/>
                                    <TextBlock Text="14:35" Style="{StaticResource OutgoingMessageTimeStyle}"/>
                                </StackPanel>
                            </Border>

                            <!-- Входящее сообщение -->
                            <Border Style="{StaticResource IncomingMessageStyle}">
                                <StackPanel>
                                    <TextBlock Text="Тоже все хорошо! Может встретимся на выходных?" 
                                               Style="{StaticResource MessageTextStyle}"/>
                                    <TextBlock Text="14:40" Style="{StaticResource MessageTimeStyle}"/>
                                </StackPanel>
                            </Border>

                            <!-- Исходящее сообщение -->
                            <Border Style="{StaticResource OutgoingMessageStyle}">
                                <StackPanel>
                                    <TextBlock Text="Отличная идея! Давай в субботу встретимся в парке в 2 часа?" 
                                               Style="{StaticResource OutgoingMessageTextStyle}"/>
                                    <TextBlock Text="14:42" Style="{StaticResource OutgoingMessageTimeStyle}"/>
                                </StackPanel>
                            </Border>

                        </StackPanel>
                    </ScrollViewer>

                    <!-- Поле ввода сообщения -->
                    <Border Grid.Row="2" Background="Transparent" Padding="20,15">
                        <DockPanel>
                            <Button DockPanel.Dock="Right" 
                                    Style="{StaticResource SendButtonStyle}" 
                                    Margin="15,0,0,0"/>
                            <TextBox x:Name="MessageInput" 
                                     Style="{StaticResource MessageInputStyle}"
                                     Text="Введите сообщение..."/>
                        </DockPanel>
                    </Border>

                </Grid>

            </Border>

            <!-- Область сообщений --><!--
            <Border Grid.Row="1" Background="#F8F8F8">
                <DockPanel>
                    --><!-- Список сообщений --><!--
                    <ScrollViewer x:Name="MessagesScrollViewer" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="MessagesPanel" Margin="20"/>
                    </ScrollViewer>
                </DockPanel>
            </Border>-->

            <!-- Поле ввода сообщения -->
            <!--<Border Grid.Row="2" Background="White" Padding="20,15">
                <DockPanel>
                    <Button DockPanel.Dock="Right" 
                           Style="{StaticResource SendButtonStyle}" 
                           Margin="10,0,0,0"
                           Click="SendButton_Click"/>
                    <TextBox x:Name="MessageInput" 
                            Style="{StaticResource MessageInputStyle}"
                            Text="Сообщение..."
                            Foreground="Gray"
                            KeyDown="MessageInput_KeyDown"
                            GotFocus="MessageInput_GotFocus"
                            LostFocus="MessageInput_LostFocus"/>
                </DockPanel>
            </Border>-->
        </Grid>
    </Grid>
</Page>
