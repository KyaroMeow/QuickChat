<Page x:Class="QuickChatApp.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QuickChatApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Вход">

    <Grid>
        <TextBlock Text="QuickChat" Style="{StaticResource AppTitleStyle}" HorizontalAlignment="Center" />
        <Image />
        <Border CornerRadius="15"
                Background="White"
                Width="350"
                Padding="40"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <DockPanel>
                <TextBlock Text="Авторизация" Style="{StaticResource AuthTitleStyle}" DockPanel.Dock="Top"/>

                <TextBox x:Name="UsernameBox"
                         Style="{StaticResource RoundedTextBoxStyle}"
                         Tag="Логин"
                         GotFocus="TextBox_GotFocus"
                         LostFocus="TextBox_LostFocus" DockPanel.Dock="Top"/>

                <PasswordBox x:Name="PasswordBox"
                             Style="{StaticResource RoundedPasswordBoxStyle}"
                             Tag="Пароль"
                             GotFocus="PasswordBox_GotFocus"
                             LostFocus="PasswordBox_LostFocus"/>

                <Button Content="Войти"
                        Style="{StaticResource LoginButtonStyle}"
                        Click="LoginButton_Click"/>

            </DockPanel>
        </Border>
                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10">
                    <Run Text="У вас нет аккаунта?" Foreground="#7C7C7C" FontWeight="SemiBold"/> <LineBreak/>
                    <Hyperlink Click="RegisterHyperlink_Click" Style="{StaticResource LinkTextStyle}">Зарегистрироваться</Hyperlink>
                </TextBlock>
    </Grid>
</Page>
