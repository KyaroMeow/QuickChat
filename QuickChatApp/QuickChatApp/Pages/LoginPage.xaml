<Page x:Class="QuickChatApp.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QuickChatApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Вход">

    <Grid>
        <TextBlock HorizontalAlignment="Center" Text="QuickChat" Style="{StaticResource AppTitleStyle}"/>
        <StackPanel VerticalAlignment="Center" Background="#FFFFFF">
            <TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" Text="Авторизация" VerticalAlignment="Top" Foreground="#5C9EAD" FontWeight="Bold" Style="{StaticResource AuthTitleStyle}"/>
            <TextBox Style="{StaticResource InputFieldStyle}" Text="Логин"
                 Tag="Логин"
                 GotFocus="TextBox_GotFocus"
                 LostFocus="TextBox_LostFocus"/>
            <PasswordBox x:Name="PasswordBox" Width="120" />
        </StackPanel>

    </Grid>
</Page>
